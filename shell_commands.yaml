upload_prusa_snapshot: >
  curl -X PUT "https://connect.prusa3d.com/api/v1/printers/{{printer_id}}/snapshots"
  -H "Authorization: Bearer {{api_token}}"
  -H "Content-Type: image/jpeg"
  --data-binary "@{{ filename }}"
